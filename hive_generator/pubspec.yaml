name: hive_generator_plus
description: Extension for Hive. Automatically generates TypeAdapters to store any class.
version: 5.0.0
homepage: https://github.com/hivedb/hive/tree/master/hive_generator
documentation: https://docs.hivedb.dev/

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  build: ^3.0.2
  source_gen: ^3.0.0
  hive: ^2.2.3
  source_helper: ^1.3.7
  analyzer: ^8.1.1

dev_dependencies:
  test: ^1.26.3
  build_test: ^3.3.2
  build_runner: ^2.7.0
